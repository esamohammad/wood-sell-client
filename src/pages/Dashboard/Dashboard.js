import React from 'react';
import useTitle from '../../hooks/useTitle';

const Dashboard = () => {
    useTitle('Dashboard');
    return (
        <div>
            Dashboard
        </div>
    );
};

export default Dashboard;